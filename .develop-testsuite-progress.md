# Develop Test Suite Progress

**Use case**: UC-SDWAN-06 QoS Priority Under WAN Contention
**Framework**: pytest-bdd

- [x] 1. Check for existing BDD scenarios — none found
- [x] 2. Create draft BDD scenarios
  - [x] Map preconditions → Background (3 Given steps: appliance, network, traffic generators)
  - [x] Map main success scenario → UC-SDWAN-06-Main scenario
  - [x] Map success guarantees → conferencing SLO check (MOS >= 3.5)
  - [x] Map extensions → 1.a and 1.b out of scope (downstream not supported in current topology)
  - [x] Map minimal guarantees → failure propagation via existing QoE assertions
  - [x] Map data variations → single scenario (conferencing only for now)
- [x] 3–10. **Scenario: UC-SDWAN-06-Main** (Upstream WAN Saturation):
  - [x] 3. Search for existing steps
    - `Given the SD-WAN appliance is operational with dual WAN connectivity` → EXISTS
    - `Given the network conditions are set to "cable_typical" on all WAN links` → EXISTS
    - `Given traffic generators are available on both sides of the appliance` → NEW
    - `Given network operations verifies that both WAN links are in UP state` → EXISTS
    - `When network operations starts 85 Mbps of best-effort upstream background traffic through the appliance` → NEW
    - `When the remote worker starts a "conferencing" session through the appliance` → EXISTS
    - `Then the remote worker confirms the "conferencing" session remains functional within the continuity SLO` → EXISTS
    - `When network operations stops the upstream background traffic` → NEW
  - [x] 4. Update draft to final; verify coverage
    - [x] For each step: automation exists? → 3 new steps implemented
    - [x] For each step: unit test exists? → 6 new unit tests (pass + fail per step)
    - [x] For each step: validation complete? → yes
  - [x] **New step: traffic_generators_available**
    - [x] 5. Implement step definition (sdwan_steps.py)
    - [x] 6. Create unit tests (test_sdwan_steps.py: 2 tests)
    - [x] 7. Execute unit tests → PASSED
    - [x] 8. Fix if failures → N/A
    - [x] 9. Repeat until verified → DONE
  - [x] **New step: network_ops_starts_upstream_traffic**
    - [x] 5. Implement step definition (sdwan_steps.py)
    - [x] 6. Create unit tests (test_sdwan_steps.py: 2 tests)
    - [x] 7. Execute unit tests → PASSED
    - [x] 8. Fix if failures → N/A
    - [x] 9. Repeat until verified → DONE
  - [x] **New step: network_ops_stops_upstream_traffic**
    - [x] 5. Implement step definition (sdwan_steps.py)
    - [x] 6. Create unit tests (test_sdwan_steps.py: 2 tests)
    - [x] 7. Execute unit tests → PASSED
    - [x] 8. Fix if failures → N/A
    - [x] 9. Repeat until verified → DONE
  - [x] 10. Run this scenario → PASSED (1 passed, 29 deselected in 42s)
- [x] **Exit checklist**:
  - [x] For each new pytest-bdd step: `tests/unit/test_step_defs/test_sdwan_steps.py` has tests (pass + fail)
  - [x] No dead/weak validations: each unit test asserts specific arguments and context state
  - [x] Log-reading steps constrain queries to scenario time window → N/A (no log-reading steps)
